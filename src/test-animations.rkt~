#lang racket
(require picturing-programs)

(require "graph.rkt")

(define (render model)
  (cond [(equal? model #t)
         (rectangle 700 400 "solid" "green")]
        [else (rectangle 700 400 "solid" "red")]))

(define (mouse-handler model x y event)
  (cond [(in-ellipse? x y